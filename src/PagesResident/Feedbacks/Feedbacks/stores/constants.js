const NS_USER = "createFeedback";

const SET_LOADING = `${NS_USER}/setLoading`;
const SAVE_LOADING = `${NS_USER}/saveLoading`;

const CREAT_FEEDBACKS = `${NS_USER}/createFeedback`;
export { SET_LOADING, SAVE_LOADING, CREAT_FEEDBACKS };
