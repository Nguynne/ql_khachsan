const FORM = "form";

const SET_LOADING = `${FORM}/setLoading`;
const SAVE_LOADING = `${FORM}/saveLoading`;

const CREAT_FORM = `${FORM}/createForm`;

export { SET_LOADING, SAVE_LOADING, CREAT_FORM };
